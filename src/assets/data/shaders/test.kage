//go:build ignore

//kage:unit pixels

package main

var Test int

func Fragment(dstPos vec4, srcPos vec2, color vec4) vec4 {
	for i := 0; i < Test; i += 1 {
		color.r += 1
	}
	return imageSrc0At(srcPos) * color
}

